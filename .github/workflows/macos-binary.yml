name: macOS Binary

on:
  workflow_dispatch:

jobs:
  build:
    name: Build release binary
    runs-on: macos-15
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Swift
        uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.2"

      - name: Build release artifact
        run: |
          swift build --configuration release --product arstdhneio
          BIN_PATH=$(swift build --configuration release --show-bin-path)
          echo "BIN_PATH=$BIN_PATH" >> "$GITHUB_ENV"

      - name: Show build output
        run: |
          echo "BIN_PATH=$BIN_PATH"
          ls -la "$BIN_PATH"
          find "$BIN_PATH" -maxdepth 1 -type f -print

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: arstdhneio-macos-binary
          path: ${{ env.BIN_PATH }}/arstdhneio
          if-no-files-found: error
